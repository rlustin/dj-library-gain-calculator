name: dj-library-gain-calculator
version: "0.1.0"
args:
    - input:
        help: The input Traktor library file to use
        required: true
        index: 1
    - output:
        help: The output Traktor library file to write or - for stdout
        required: false
        index: 2
